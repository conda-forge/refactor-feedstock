{% set name = "refactor" %}
{% set version = "0.4.3" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/refactor-{{ version }}.tar.gz
  sha256: e259f192ed2e60bce31a2f968e3c7f8d47bc8c700ba15691399a181db6fe3e5c

build:
  number: 0
  noarch: python
  entry_points:
    - refactor = refactor.__main__:main
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.9
    - setuptools>=40.8.0
  run:
    - python >=3.9

test:
  imports:
    - refactor
  commands:
    - pip check
    - refactor --help
  requires:
    - pip

about:
  home: https://github.com/isidentical/refactor
  summary: AST based source code refactor interface
  doc_url: https://refactor.readthedocs.io/en/latest/
  license: BSD-3-Clause
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - thewchan
